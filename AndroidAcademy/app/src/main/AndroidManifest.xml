<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yxsoong.project.androidacademy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Lesson1Activity" />
        <activity android:name=".GettingStartedActivity" />
        <activity
            android:name=".CreateProjectActivity"
            android:label="@string/title_activity_create_project" />
        <activity android:name=".Lesson2Activity" />
        <activity android:name=".AndroidEmulatorActivity" />
        <activity android:name=".AndroidManifestActivity" />
        <activity android:name=".AndroidTermsActivity"></activity>
    </application>

</manifest>